import React from 'react';
import {Link} from 'react-router-dom';
// import logo from '../logo.svg';

const Header = () => (
<header className="App-header">
  <h1 className="App-title">Towers of hanoi</h1>
</header>

);

export default Header;
